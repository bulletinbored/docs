{
  "name": "bulletinbored-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docmd dev",
    "build": "docmd build",
    "preview": "npx serve site",
    "init": "docmd init",
    "doctor": "docmd doctor",
    "validate": "docmd validate",
    "migrate": "docmd migrate"
  },
  "engines": {
    "node": ">=18.0"
  },
  "devDependencies": {
    "@docmd/core": "^0.9.0"
  }
}
